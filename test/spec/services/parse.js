'use strict';

describe('Parse: ApiWrapper', function () {

  // load the controller's module
  beforeEach(module('pawtrailsApp'));

  it('should create a new account for users on signup', function () {
    expect(false).toBeFalsy();
  });
});